import 'package:flutter/material.dart';

const myWhiteStyle =
    TextStyle(fontWeight: FontWeight.bold, color: Colors.white, fontSize: 32);

const myBlackeStyle = TextStyle(
    fontWeight: FontWeight.bold,
    color: Color.fromARGB(255, 0, 0, 0),
    fontSize: 32);

/// button color
const topButtonColor = Color(0xffD2D3DA);

/// secondary buttoncolor
const secondaryButtonColor = Color(0xff4B5EFC);

const myNumberStyle = TextStyle(fontWeight: FontWeight.w300, fontSize: 90);
